# Metric Atlas

Mapa 2×2 (lienzo tipo board), filtros por variables y ficha por métrica. Datos en `src/data/metrics.ts`.

```bash
npm install
npm run dev
```

- `/` — mapa + panel “cómo medir”
- `/metrics/[id]` — ficha
- `GET /api/metrics` — JSON `{ metrics }`
